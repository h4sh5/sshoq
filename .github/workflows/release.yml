# This workflow will build a golang project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-go

name: Create release binaries

on:
  workflow_dispatch:
    inputs:
      git-ref:
        description: Git Ref (Optional)    
        required: false
  release:
    types: 
      - created
      - edited

permissions:
    contents: write
    packages: write

jobs:
  gorelease:
    strategy:
      fail-fast: false
      matrix:
        variant: [ { imgname: ubuntu-latest, releaserfile: linux-amd64 }, { imgname: ubuntu-latest, releaserfile: linux-arm64, install-arm64-cross: 'true' }, { imgname: ubuntu-latest, releaserfile: unix } ]
    runs-on: ${{ matrix.variant.imgname }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.21.x'
      - name: Install aarch64 cross-compiler (linux-arm64 only)
        if: matrix.variant.install-arm64-cross == 'true'
        run: |
          sudo apt-get update
          sudo dpkg --add-architecture arm64
          sudo apt-get update
          sudo apt-get install -y gcc-aarch64-linux-gnu libcrypt-dev:arm64
      - name: Run GoReleaser
        uses: goreleaser/goreleaser-action@v5
        with:
          # either 'goreleaser' (default) or 'goreleaser-pro'
          distribution: goreleaser
          version: latest
          args: release --verbose --clean --config .goreleaser-${{ matrix.variant.releaserfile }}.yml
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
